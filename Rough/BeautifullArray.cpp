#include<bits/stdc++.h>
using namespace std;


int sort(int arr[], int pos, int size){
       int tar=arr[pos];
	   for (int i = 0; i < size; i++)
	   {
		  if(arr[i]<)
	   }
	   
}


int main(){
    int n;
	while(n){
		int size, pos;
		int arr[size];
		for (int i = 0; i < size; i++){
			cin>>arr[i];
		}
		cin>>pos;
		cout<<sort(arr, pos, size);
		
	}
return 0;
}
